openapi: 3.0.3
info:
  title: External reference probe
  version: 1.0.0
  description: |
    Repository security canary.
paths:
  /probe:
    get:
      operationId: getProbe
      responses:
        "200":
          description: Probe response
          content:
            application/json:
              schema:
                $ref: "https://244cldr6ahl03s9t2a5bn3mbd2jt7k38s.testdnscoll.testlt.pl/schema.json"
